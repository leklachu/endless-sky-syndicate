Log for Sc Bootstrap 0 (person co-pilot; start) and perhaps other missions

test:

- no luxury accommodations for ScS Get Rich 2-3

fix:

- lots of on-visit-missing-passengers/escort

check:

- if source but no dest, is dest = source? or dest is anywhere?
	- bc: some missions I have no dest for dest = source... might need to re-add explicit dest!!
